{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the window is allowed to ask the Rust side for. Nothing beyond Tauri's own core, because nothing in echo needs more yet.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
