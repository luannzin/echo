/**
 * The one place a URL is written down.
 *
 * `SITE` is what the canonical tags and the social card resolve against; `APP` is the built web app
 * on its own subdomain, which is the page's primary call to action and the reason a visitor no
 * longer has to clone anything to try echo.
 */
export const REPO = "https://github.com/luannzin/echo";
export const SITE = "https://useecho.dev";
export const APP = "https://app.useecho.dev";

/** Where a reader who wants to say thanks goes. Nothing is sold here: see `support.tsx`. */
export const COFFEE = "https://buymeacoffee.com/luannzin";
