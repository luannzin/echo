/**
 * Local mode is single-workspace. The column exists on every row so hosted multi-workspace mode is
 * an additive change rather than a schema rewrite.
 */
export const DEFAULT_WORKSPACE_ID = "00000000-0000-0000-0000-000000000001";
